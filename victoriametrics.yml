global:
  scrape_interval: 30s      # reduced from 5s — prevents hammering the endpoint
  scrape_timeout: 10s       # gives the service time to respond

scrape_configs:
  - job_name: "sensor"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["sensor-service:8000"]
